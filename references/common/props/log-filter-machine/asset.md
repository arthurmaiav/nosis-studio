# Log Filter Machine

The log filter machine is a physical visual metaphor for Worker's three software-filter jobs. It has exactly three modules and one repairable brass fitting on the third module.

The master sheet locks the casing, module count, pipework, materials, and service fitting. Module numbers and indicator states are added in post so the story can show `28`, `29`, and `30` exactly without trusting generated text.
